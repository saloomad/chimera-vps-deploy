# Hermes VPS Chimera Bootstrap

Verified local Hermes home:

- `/root/.hermes/`

Verified current config:

- provider: `minimax`
- model: `MiniMax-M2.5-HighSpeed`

Use the same orchestration rule:

`plan -> execute -> review -> repeat`

Current honest limit:

- Hermes does not yet have a verified full shared Chimera skill pack like Kimi
- install shared Chimera-oriented skills into `/root/.hermes/skills/` and keep the bootstrap explicit until that is proven
