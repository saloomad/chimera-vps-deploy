---
name: hook-opportunity-detector
description: Detect when a repeated reminder should become an automatic event reaction or stronger enforcement surface.
compatibility: opencode
metadata:
  platform: opencode
  workflow: enforcement
---

Use this when the user wants automatic triggers, event guards, or proof that something fires consistently.

Choose the strongest honest surface available on this platform:

1. rules
2. commands
3. agents
4. permissions
5. review gates

If the task is actually about Claude Code or OpenClaw, call that out plainly and route to the platform-specific docs instead of pretending OpenCode owns the hook.
